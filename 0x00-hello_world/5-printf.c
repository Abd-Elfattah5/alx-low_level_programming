#include <stdio.h>
/**
 *main - Entry point
 *Return: always 0 (success)
 *this is a function to print a sentence using printf function
 */
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
